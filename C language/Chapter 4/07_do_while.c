#include <stdio.h>

int main(){
    int i = 0;
    do
    {
       printf("The value of i is %d\n",i);
       i++;
    } while (i<4);
    
    return 0;
}

// in simple we can say do - while loop = while loop which exexcutes at least once!
